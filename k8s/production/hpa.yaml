apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: backforge-api
  namespace: backforge
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backforge-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
