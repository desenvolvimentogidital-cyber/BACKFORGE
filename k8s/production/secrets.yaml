apiVersion: v1
kind: Secret
metadata:
  name: backforge-secrets
  namespace: backforge
type: Opaque
stringData: {}
# Create real secrets out-of-band, for example:
# kubectl -n backforge create secret generic backforge-secrets \
#   --from-literal=DATABASE_URL="$DATABASE_URL" \
#   --from-literal=REDIS_URL="$REDIS_URL" \
#   --from-literal=JWT_SECRET="$JWT_SECRET" \
#   --from-literal=STRIPE_SECRET_KEY="$STRIPE_SECRET_KEY" \
#   --from-literal=STRIPE_WEBHOOK_SECRET="$STRIPE_WEBHOOK_SECRET" \
#   --from-literal=STRIPE_PRICE_BASIC="$STRIPE_PRICE_BASIC" \
#   --from-literal=STRIPE_PRICE_PRO="$STRIPE_PRICE_PRO" \
#   --from-literal=S3_ACCESS_KEY="$S3_ACCESS_KEY" \
#   --from-literal=S3_SECRET_KEY="$S3_SECRET_KEY"
