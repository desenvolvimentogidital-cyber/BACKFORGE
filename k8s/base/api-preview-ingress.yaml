apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backforge-api-preview
  labels:
    app.kubernetes.io/name: backforge-api
    app.kubernetes.io/component: preview-ingress
    app.kubernetes.io/part-of: backforge
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: 10m
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - preview.backforge.example.com
      secretName: backforge-api-preview-tls
  rules:
    - host: preview.backforge.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backforge-api-preview
                port:
                  number: 80
